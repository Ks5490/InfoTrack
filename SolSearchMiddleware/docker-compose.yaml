version: "3.9"

services:
  api:
    image: conveyancing_middleware
    container_name: conveyancing_middleware
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: always